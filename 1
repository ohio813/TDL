uses a "VirtualBox" "loaded vulnerable driver" which has a proper x64 signature to then exploit it and gain kernel mode privs and proceeds to manually load further drivers as the purpose of the program          If in file    hfiref0x/TDL/blob/master/Source/Furutaka/main.c
